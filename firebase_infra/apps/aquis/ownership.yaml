app: aquis
rules_paths:
  - "/users/{uid}/health_raw/{dateKey}"
  - "/users/{uid}/health_derived/{dateKey}"
  - "/users/{uid}/readiness/{dateKey}"
  - "/users/{uid}/readiness_overrides/{dateKey}"
  - "/dailyTrainingRecords/{id}"
  - "/attendanceResponses/{id}"
  - "/coachNotes/{id}"
  - "/aiSubscriptions/{uid}"
  - "/healthDailyMetrics/{id}"
  - "/healthWorkoutMetrics/{id}"
  - "/healthSyncState/{uid}"
  - "/healthCoachDailyAggregates/{id}"
  - "/completedSessions/{id}"
index_collection_groups:
  - "healthCoachDailyAggregates"
  - "healthDailyMetrics"
  - "healthWorkoutMetrics"
storage_paths:
